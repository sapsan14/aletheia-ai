Вычислять coverage = evaluated / total_rules. Сохранять в БД.

**Acceptance Criteria:**
- Coverage вычисляется при генерации ответа
- Coverage сохраняется в БД
- Coverage отдаётся в `/api/ai/verify/:id`

См. `docs/en/PLAN_PHASE4.md`.
