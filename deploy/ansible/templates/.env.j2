# Aletheia AI — generated by Ansible, do not edit manually
# Managed from deploy/ansible/templates/.env.j2

# PostgreSQL (docker-compose)
POSTGRES_DB={{ postgres_db | default('aletheia') }}
POSTGRES_USER={{ postgres_user | default('aletheia') }}
POSTGRES_PASSWORD={{ postgres_password | default('local') }}

# Backend (via docker-compose environment)
OPENAI_API_KEY={{ openai_api_key | default('') }}
AI_ALETHEIA_TSA_MODE={{ ai_aletheia_tsa_mode | default('real') }}
AI_ALETHEIA_TSA_URL={{ ai_aletheia_tsa_url | default('http://timestamp.digicert.com') }}
CORS_ALLOWED_ORIGINS={{ cors_allowed_origins | default('http://localhost:3000') }}

# Frontend build (NEXT_PUBLIC_API_URL baked at build time; empty = relative /api for ngrok)
# Use default(..., true) so that empty string is preserved when ngrok sets next_public_api_url: ""
NEXT_PUBLIC_API_URL={{ next_public_api_url | default('http://localhost:8080', true) }}
