# Backend API URL (required for Send and Verify to work)
# Copy to .env.local: cp .env.example .env.local
# When running locally: set to http://localhost:8080 (backend: cd backend && mvn spring-boot:run)
NEXT_PUBLIC_API_URL=http://localhost:8080
