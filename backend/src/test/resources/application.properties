# Test signing key (PEM in classpath) so context loads without external file
ai.aletheia.signing.key-path=classpath:test-signing-key.pem

# Use in-memory H2 for tests (isolated, no file I/O)
spring.datasource.url=jdbc:h2:mem:aletheia
